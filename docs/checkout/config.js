window.FLOPPY_CHECKOUT_CONFIG = Object.freeze({
  // Replace this staging URL with the production endpoint before launch.
  // The backend must allow https://floppysoftware.com (without a path) via CORS.
  checkoutEndpoint: "https://46pqyel5tc.execute-api.us-east-1.amazonaws.com/v1/checkout",
});
