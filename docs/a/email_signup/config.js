window.FLOPPY_EMAIL_SIGNUP_CONFIG = Object.freeze({
  // The backend must allow https://floppysoftware.com (without a path) via CORS.
  downloadEmailEndpoint: "https://2a4cjoxwud.execute-api.us-east-1.amazonaws.com/v1/emails/download",
});
